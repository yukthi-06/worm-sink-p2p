# User Prompt
proceed to implement folder sharing

## Changes Implemented

### Virtual mapping
- Created [VirtualFileMapper.java](file:///x:/Projects_X/0_Active/1_Java_Active/AI_Assisted/WormSink_Java_GITHUB/wormsink-transfer/src/main/java/org/wormsink/transfer/VirtualFileMapper.java) to translate virtual contiguous offsets to individual files in a directory hierarchy. Handles nested directories, relativized paths, pre-allocation, and multi-file chunk reads/writes.

### Directory integrity check
- Added a `calculateFolderSha256` method to [ChunkingEngine.java](file:///x:/Projects_X/0_Active/1_Java_Active/AI_Assisted/WormSink_Java_GITHUB/wormsink-transfer/src/main/java/org/wormsink/transfer/ChunkingEngine.java) that computes a sequential cryptographic SHA-256 digest of all files sorted deterministically.

### Core engine upgrade
- Updated [TransferEngine.java](file:///x:/Projects_X/0_Active/1_Java_Active/AI_Assisted/WormSink_Java_GITHUB/wormsink-transfer/src/main/java/org/wormsink/transfer/TransferEngine.java) to:
  - Generate and serialize folder manifests containing files, sizes, and relative paths.
  - Parse manifests on the receiver side and recursively pre-allocate sparse directories and files.
  - Read and write data chunks seamlessly using the `VirtualFileMapper`.
  - Validate folder integrity upon transfer completion or startup checking.

### CLI integration
- Modified [WormSinkCli.java](file:///x:/Projects_X/0_Active/1_Java_Active/AI_Assisted/WormSink_Java_GITHUB/wormsink-cli/src/main/java/org/wormsink/cli/WormSinkCli.java) to accept folder paths as inputs, verify their existence, and correctly format console output strings.

## Verification Status
- Compilation and structural logic are complete.
- Build the project using `./gradlew clean build` and verify sharing directories via command line!
