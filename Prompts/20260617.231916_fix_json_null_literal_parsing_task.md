- [x] Identify root cause: `SimpleJson.getField` returns Java String `"null"` for JSON `null` literals.
- [x] Fix `SimpleJson.getField` to return Java `null` when matched value is `"null"`.
- [ ] Rebuild and verify transfer completes end-to-end.
