Now signalling server shows no error but sender and receiver logs below

java -jar .\wormsink-cli\build\libs\wormsink-1.0.0-fatjar.jar send "D:\sw\sqlite-tools-win-x64-3510000.zip"  --signaling-url=http://127.0.0.1:8787
Preparing file...
Generating metadata...
Transfer code:

  amused-type-lazy-model

Waiting for receiver...
Starting transfer of: sqlite-tools-win-x64-3510000.zip (6.1 MB)
Connecting to receiver...

Receiver connected. Initializing transfer...

Receiver disconnected.

Transfer failed: CONNECTION: CLOSED
VM attach current thread failed
Failed to attach thread 8488
The batch file cannot be found.
---------------------------------------------------------


java -jar .\wormsink-cli\build\libs\wormsink-1.0.0-fatjar.jar receive amused-type-lazy-model --signaling-url=http://127.0.0.1:8787
Connecting...
Verifying metadata...
Connecting to peer...

Peer connected. Fetching file...

Download failed: CONNECTION: CLOSED
VM attach current thread failed
Failed to attach thread 8044

## Goal
Diagnose and fix the abrupt closure of WebRTC connections by ensuring original exceptions are not overwritten by connection close status in `TransferEngine`, and log detailed exceptions to stderr for debugging.

## Proposed Changes

### [TransferEngine](file:///x:/Projects_X/0_Active/1_Java_Active/AI_Assisted/WormSink_Java_GITHUB/wormsink-transfer/src/main/java/org/wormsink/transfer/TransferEngine.java)
- Update `onConnectionStateChange` to only assign `errorMessage = state` if `errorMessage == null`. This prevents the original exception (e.g. from decoding or processing frames) from being overwritten by subsequent connection teardown states.
- Update `onDataChannelStateChange` for `RTCDataChannelState.CLOSED` to only assign `errorMessage` if `errorMessage == null`.
- Update `onMessageReceived` to print the stack trace to `System.err` if `handleProtocolFrame` throws an exception, helping diagnose parsing or metadata exceptions.

## Verification Plan
1. Rebuild the project using `./gradlew :wormsink-cli:fatJar`.
2. Run the sender and receiver commands locally.
3. Inspect stderr/console output for the actual exceptions/errors causing the failure.
