# User Prompt

> proceed

---

# Task: Synchronize Resumed Sender Progress

- [x] Modify `handleProtocolFrame(METADATA)` in `TransferEngine.java` to send `RESUME` frame when resuming
- [x] Modify `handleProtocolFrame(RESUME)` in `TransferEngine.java` to set progress and report it
- [x] Verify build and functionality
