again it looks like sender and receiver are stuck.
However the signalling server logs a request every one second(with response 200)/

## Changes Completed

- Created [run-send.bat](file:///x:/Projects_X/0_Active/1_Java_Active/AI_Assisted/WormSink_Java_GITHUB/run-send.bat): Automatically compiles/installs the standard distribution via `installDist` and runs the sender using standard separate classpath directories.
- Created [run-recv.bat](file:///x:/Projects_X/0_Active/1_Java_Active/AI_Assisted/WormSink_Java_GITHUB/run-recv.bat): Runs the receiver using standard separate classpath directories.

## Next Steps
1. In the sender terminal, run:
   ```cmd
   run-send.bat
   ```
2. In the receiver terminal, copy the code printed by the sender and run:
   ```cmd
   run-recv.bat <code-here>
   ```
3. Observe if the transfer successfully connects and completes.
