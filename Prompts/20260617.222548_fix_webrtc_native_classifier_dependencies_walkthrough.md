# User Prompt

* What went wrong:
Could not determine the dependencies of task ':wormsink-cli:fatJar'.
> Could not resolve all files for configuration ':wormsink-cli:runtimeClasspath'.
   > Could not find dev.onvoid.webrtc:webrtc-java-windows-x86_64:0.14.0.
...

---

# Walkthrough - Fix WebRTC Native Classifier Dependencies

We have corrected the JNI dependencies coordinates format inside the build script.

## Changes Made

1.  **[wormsink-webrtc/build.gradle.kts](file:///x:/Projects_X/0_Active/1_Java_Active/AI_Assisted/WormSink_Java_GITHUB/wormsink-webrtc/build.gradle.kts):** Corrected the dependencies notation using `group:name:version:classifier` syntax so Gradle pulls JNI libraries correctly.
    - `runtimeOnly("dev.onvoid.webrtc:webrtc-java:0.14.0:windows-x86_64")`
    - `runtimeOnly("dev.onvoid.webrtc:webrtc-java:0.14.0:linux-x86_64")`
    - `runtimeOnly("dev.onvoid.webrtc:webrtc-java:0.14.0:macos-x86_64")`

## Verification Commands

To rebuild the fat jar:
```cmd
gradle :wormsink-cli:fatJar
```
