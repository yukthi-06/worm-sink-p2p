there is no error, but both sides are waiting on the other.

java -jar .\wormsink-cli\build\libs\wormsink-1.0.0-fatjar.jar receive flavor-tool-legend-way --signaling-url=http://127.0.0.1:8787
Connecting...
Verifying metadata...
Connecting to peer...
VM attach current thread failed
Failed to attach thread 24092


java -jar .\wormsink-cli\build\libs\wormsink-1.0.0-fatjar.jar send "D:\sw\sqlite-tools-win-x64-3510000.zip"  --signaling-url=http://127.0.0.1:8787
Preparing file...
Generating metadata...
Transfer code:

  flavor-tool-legend-way

Waiting for receiver...
Starting transfer of: sqlite-tools-win-x64-3510000.zip (6.1 MB)
Connecting to receiver...

## Goal
Fix the JNI thread attachment failure and subsequent hang by preventing the receiver (and sender) from adding remote ICE candidates before the remote description has been set.

## Proposed Changes

### [PeerConnectionManager](file:///x:/Projects_X/0_Active/1_Java_Active/AI_Assisted/WormSink_Java_GITHUB/wormsink-webrtc/src/main/java/org/wormsink/webrtc/PeerConnectionManager.java)
- Add a `private volatile boolean isRemoteDescriptionSet = false;` flag to track when the remote description has been successfully applied.
- Set `isRemoteDescriptionSet = true` in the `onSuccess` callback of `setRemoteDescription` (both for the offer on the receiver and the answer on the sender).
- Guard the remote ICE candidate polling task in `startIceCandidatePolling()` so that it returns immediately and does not call `peerConnection.addIceCandidate()` if `isRemoteDescriptionSet` is false.

## Verification Plan
1. Recompile the fat jar.
2. Run the sender and receiver commands locally and verify the transfer completes without JNI thread attachment failures or hanging.
