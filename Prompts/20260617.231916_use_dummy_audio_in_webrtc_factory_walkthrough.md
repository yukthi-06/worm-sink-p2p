still stuck.

FYI, I am running both on same laptop since we are just testing

## Changes Completed

### [PeerConnectionManager](file:///x:/Projects_X/0_Active/1_Java_Active/AI_Assisted/WormSink_Java_GITHUB/wormsink-webrtc/src/main/java/org/wormsink/webrtc/PeerConnectionManager.java)
- Configured the factory to use dummy audio by passing `new AudioDeviceModule(AudioLayer.kDummyAudio)` to the `PeerConnectionFactory` constructor.
- Added code to dispose of the `AudioDeviceModule` inside `close()`.

This bypasses the hardware lock conflict that happens when two instances of the application (sender and receiver) try to initialize the real system audio recording/playback devices at the same time on the same laptop.

## Next Steps
1. Run the sender via the classpath launcher:
   ```cmd
   run-send.bat
   ```
2. Run the receiver:
   ```cmd
   run-recv.bat <code-printed-by-sender>
   ```
3. Verify if they connect successfully and complete the file transfer on the same laptop.
