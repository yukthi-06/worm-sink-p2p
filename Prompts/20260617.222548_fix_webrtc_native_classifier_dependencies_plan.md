# User Prompt

* What went wrong:
Could not determine the dependencies of task ':wormsink-cli:fatJar'.
> Could not resolve all files for configuration ':wormsink-cli:runtimeClasspath'.
   > Could not find dev.onvoid.webrtc:webrtc-java-windows-x86_64:0.14.0.
...

---

# Implementation Plan - Fix WebRTC Native Classifier Dependencies

This plan addresses the resolution failure of the platform-native JNI libraries.

## User Review Required

> [!IMPORTANT]
> - We will modify `wormsink-webrtc/build.gradle.kts` to use Gradle's standard classifier dependency syntax: `group:name:version:classifier`.
> - This resolves the `webrtc-java` platform dependencies correctly from Maven Central using the core `webrtc-java` artifact with platform-specific classifiers.

## Proposed Changes

### [WormSink WebRTC]

#### [MODIFY] [build.gradle.kts](file:///x:/Projects_X/0_Active/1_Java_Active/AI_Assisted/WormSink_Java_GITHUB/wormsink-webrtc/build.gradle.kts)
- Change JNI dependency coordinates to correct classifier syntax.

---

## Verification Plan

### Automated Verification
- Rebuild fat jar: `gradle :wormsink-cli:fatJar`
