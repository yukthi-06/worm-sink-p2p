- [x] Import dummy audio module classes in `PeerConnectionManager.java`.
- [x] Configure `PeerConnectionFactory` to instantiate with `AudioLayer.kDummyAudio`.
- [x] Properly dispose of the `AudioDeviceModule` inside `close()`.
- [ ] Ask the user to run the transfer on the same laptop using the wrappers.
