# User Prompt

Now signalling server shows no error but sender and receiver logs below
...
Receiver connected. Initializing transfer...
Receiver disconnected.
Transfer failed: CONNECTION: CLOSED
...

---

# Implementation Plan - Fix Metadata Unquoted JSON Parsing

This plan addresses the connection failure during metadata exchange caused by the inability of `SimpleJson` to parse unquoted numeric values.

## User Review Required

> [!IMPORTANT]
> - We will update `SimpleJson.java` to support parsing both quoted strings and unquoted numeric/boolean values.
> - This prevents `NumberFormatException` when parsing unquoted integers/longs (such as `fileSize` or `chunkSize`) in the metadata payload.

## Proposed Changes

### [WormSink Signaling]

#### [MODIFY] [SimpleJson.java](file:///x:/Projects_X/0_Active/1_Java_Active/AI_Assisted/WormSink_Java_GITHUB/wormsink-signaling/src/main/java/org/wormsink/signaling/SimpleJson.java)
- Enhance `getField` to support unquoted value capturing.

---

## Verification Plan

### Automated Verification
- Rebuild fat jar: `gradle :wormsink-cli:fatJar`
- Run the transfer CLI again to ensure file metadata is successfully verified, chunks are requested, and transfer completes without dropping.
