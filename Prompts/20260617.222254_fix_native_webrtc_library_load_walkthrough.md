# User Prompt

sending a file gives this error

[22:22:27.87] X:\Projects_X\0_Active\1_Java_Active\AI_Assisted\WormSink_Java_GITHUB>java -jar .\wormsink-cli\build\libs\wormsink-1.0.0-fatjar.jar send "D:\sw\sqlite-tools-win-x64-3510000.zip"  --signaling-url=http://127.0.0.1:8787
...
Caused by: java.lang.RuntimeException: Load library 'webrtc-java' failed
...

---

# Walkthrough - Fix Native WebRTC Library Load Error

We have added runtime platform dependencies to the WebRTC build configuration so the JVM can resolve and unpack the required native binaries at runtime.

## Changes Made

1.  **[wormsink-webrtc/build.gradle.kts](file:///x:/Projects_X/0_Active/1_Java_Active/AI_Assisted/WormSink_Java_GITHUB/wormsink-webrtc/build.gradle.kts):** Added `runtimeOnly` configurations for:
    - `dev.onvoid.webrtc:webrtc-java-windows-x86_64:0.14.0`
    - `dev.onvoid.webrtc:webrtc-java-linux-x86_64:0.14.0`
    - `dev.onvoid.webrtc:webrtc-java-macos-x86_64:0.14.0`
    This ensures that when a fat jar is constructed, the native binaries (`.dll`, `.so`, `.dylib`) are bundled on the classpath.

## Verification Commands

To rebuild the fat jar and run the application:
1.  **Build the fat jar:**
    ```cmd
    gradle :wormsink-cli:fatJar
    ```
2.  **Run the sender client:**
    ```cmd
    java -jar .\wormsink-cli\build\libs\wormsink-1.0.0-fatjar.jar send "D:\sw\sqlite-tools-win-x64-3510000.zip" --signaling-url="http://127.0.0.1:8787"
    ```
