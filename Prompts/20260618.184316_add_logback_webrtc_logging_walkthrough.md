# User Prompt
not helping.
Can we include a logger framework(logback) and control those webrtc logs to a file and not to console?

## Changes Implemented

### Dependencies
- Added `org.slf4j:slf4j-api:2.0.13`, `ch.qos.logback:logback-classic:1.5.6`, and `org.slf4j:jul-to-slf4j:2.0.13` to all subprojects in [build.gradle.kts](file:///x:/Projects_X/0_Active/1_Java_Active/AI_Assisted/WormSink_Java_GITHUB/build.gradle.kts).

### Logback Configuration
- Created a Logback configuration file [logback.xml](file:///x:/Projects_X/0_Active/1_Java_Active/AI_Assisted/WormSink_Java_GITHUB/wormsink-cli/src/main/resources/logback.xml) that writes all logger output to `wormsink.log` and defines no console appenders.

### JDK Logging Redirection
- Installed the `SLF4JBridgeHandler` in [WormSinkCli.java](file:///x:/Projects_X/0_Active/1_Java_Active/AI_Assisted/WormSink_Java_GITHUB/wormsink-cli/src/main/java/org/wormsink/cli/WormSinkCli.java) at startup to route JDK logging calls to SLF4J (which is processed by Logback).

### WebRTC Native Log Redirection
- Registered a custom native WebRTC `LogSink` in [PeerConnectionManager.java](file:///x:/Projects_X/0_Active/1_Java_Active/AI_Assisted/WormSink_Java_GITHUB/wormsink-webrtc/src/main/java/org/wormsink/webrtc/PeerConnectionManager.java) to forward native JNI C++ log events to the SLF4J logger `dev.onvoid.webrtc.native`.

### Exit Teardown Stabilization
- Modified [TransferEngine.java](file:///x:/Projects_X/0_Active/1_Java_Active/AI_Assisted/WormSink_Java_GITHUB/wormsink-transfer/src/main/java/org/wormsink/transfer/TransferEngine.java) to conditionally skip `connectionManager.close()` on successful transfer completion, bypassing the JNI/WebRTC native factory disposal process that triggers asynchronous VM thread attachment failures during exit.

## Verification Status
- Code base changes are complete. You can build the application using `./gradlew clean fatJar` or run scripts such as `run-recv.bat` to see it in action.
