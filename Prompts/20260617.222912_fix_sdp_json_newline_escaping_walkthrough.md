# User Prompt

On java consle I get this

java -jar .\wormsink-cli\build\libs\wormsink-1.0.0-fatjar.jar send "D:\sw\sqlite-tools-win-x64-3510000.zip"  --signaling-url=http://127.0.0.1:8787
...
On signalling server I get this
[wrangler:inf] POST /session/offer 500 Internal Server Error (74ms)

---

# Walkthrough - Fix SDP JSON Newline Escaping

We have updated the simple JSON parser to properly escape and unescape newlines and tab characters.

## Changes Made

1.  **[SimpleJson.java](file:///x:/Projects_X/0_Active/1_Java_Active/AI_Assisted/WormSink_Java_GITHUB/wormsink-signaling/src/main/java/org/wormsink/signaling/SimpleJson.java):** 
    - Updated `escape(String)` to replace raw backslashes, quotes, carriage returns (`\r`), line feeds (`\n`), and tabs (`\t`) with their JSON-safe escaped equivalents.
    - Updated `getField(String, String)` to decode/unescape `\\r`, `\\n`, and `\\t` when reading incoming payloads.

## Verification Commands

To rebuild the fat jar and run the application:
1.  **Build the fat jar:**
    ```cmd
    gradle :wormsink-cli:fatJar
    ```
2.  **Run the sender client:**
    ```cmd
    java -jar .\wormsink-cli\build\libs\wormsink-1.0.0-fatjar.jar send "D:\sw\sqlite-tools-win-x64-3510000.zip" --signaling-url="http://127.0.0.1:8787"
    ```
