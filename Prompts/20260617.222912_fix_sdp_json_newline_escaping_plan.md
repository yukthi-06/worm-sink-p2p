# User Prompt

On java consle I get this

java -jar .\wormsink-cli\build\libs\wormsink-1.0.0-fatjar.jar send "D:\sw\sqlite-tools-win-x64-3510000.zip"  --signaling-url=http://127.0.0.1:8787
...
On signalling server I get this
[wrangler:inf] POST /session/offer 500 Internal Server Error (74ms)

---

# Implementation Plan - Fix SDP JSON Newline Escaping

This plan addresses the `500 Internal Server Error` on the signaling server caused by unescaped newlines in JSON payloads.

## User Review Required

> [!IMPORTANT]
> - We will update `SimpleJson.java` to properly escape and unescape newlines (`\n`, `\r`) and tab characters (`\t`).
> - This prevents the Cloudflare Worker JSON parser from throwing syntax errors when receiving multi-line SDP descriptions.

## Proposed Changes

### [WormSink Signaling]

#### [MODIFY] [SimpleJson.java](file:///x:/Projects_X/0_Active/1_Java_Active/AI_Assisted/WormSink_Java_GITHUB/wormsink-signaling/src/main/java/org/wormsink/signaling/SimpleJson.java)
- Update `escape` and `getField` to support newlines and tab serialization.

---

## Verification Plan

### Automated Verification
- Rebuild fat jar: `gradle :wormsink-cli:fatJar`
- Test running the transfer CLI to ensure it successfully transmits the offer SDP without a `500` error.
