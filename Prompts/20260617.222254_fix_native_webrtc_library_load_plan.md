# User Prompt

sending a file gives this error

[22:22:27.87] X:\Projects_X\0_Active\1_Java_Active\AI_Assisted\WormSink_Java_GITHUB>java -jar .\wormsink-cli\build\libs\wormsink-1.0.0-fatjar.jar send "D:\sw\sqlite-tools-win-x64-3510000.zip"  --signaling-url=http://127.0.0.1:8787
...
Caused by: java.lang.RuntimeException: Load library 'webrtc-java' failed
...

---

# Implementation Plan - Fix Native WebRTC Library Load Error

This plan addresses the `UnsatisfiedLinkError` / resource loading error when running the fat jar.

## User Review Required

> [!IMPORTANT]
> - We will add platform-specific JNI runtime dependencies for Windows, Linux, and macOS to `wormsink-webrtc/build.gradle.kts`.
> - This bundles the native binaries (`.dll`, `.so`, `.dylib`) directly inside the fat jar classpath resources, allowing cross-platform executions out-of-the-box.

## Proposed Changes

### [WormSink WebRTC]

#### [MODIFY] [build.gradle.kts](file:///x:/Projects_X/0_Active/1_Java_Active/AI_Assisted/WormSink_Java_GITHUB/wormsink-webrtc/build.gradle.kts)
- Add `runtimeOnly` native platform dependency configurations.

---

## Verification Plan

### Automated Verification
- Rebuild fat jar: `gradle :wormsink-cli:fatJar`
- Test running the fat jar to ensure initialization of `PeerConnectionFactory` succeeds.
